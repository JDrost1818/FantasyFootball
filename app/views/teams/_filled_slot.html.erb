<%= form_for(@game) do |f| %>
	<tr>
      <td><%= @player.position %></td>
      <td><%= link_to @player.full_name, 
                         	{ :action => "show", :team_id => @team.id, :id => @player.id, :controller=>"players" }, 
                         	{ :align=>"left"}%><%= ", #{@player.nfl_team}"%></td>
      <td align="center"><%= link_to "Drop",
      			 { :action => "rem_player", :id => params[:id], :player_id  => @player.id, :tab => "Roster" }, 
      			 { :align=>"left", :class=>"btn btn-sm btn-danger" } %></td>
    </tr>
<% end %>